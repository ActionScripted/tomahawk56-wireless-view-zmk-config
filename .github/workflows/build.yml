name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Keep the workflow branch aligned with the ZMK revision in config/west.yml.
    uses: Tomahawk-Keyboards/zmk/.github/workflows/build-user-config.yml@modify_backlight_layer
